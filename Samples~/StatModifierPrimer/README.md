# Stat Modifier Primer

This pure C# example builds a base damage stat, applies one additive and one multiplicative modifier, and returns the resolved value.

Call `StatModifierPrimer.BuildExampleDamage()` from an EditMode test or application bootstrap. The example is isolated in its own assembly and keeps Unity engine references disabled.
